spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ticket_service

    server:
      port: 8081

      eureka:
        instance:
          prefer-ip-address: true
        client:
          fetch-registry: true
          register-with-eureka: true
          service-url:
            defaultZone: http://localhost:8999/eureka